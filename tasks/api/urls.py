# from django.urls import path
# from .views import TasksView, SingleTasksView, AssignedToMe, Reviewing

# urlpatterns = [
#     path('', TasksView.as_view(), name='tasks'),
#     path('<int:pk>', SingleTasksView.as_view(), name='single_task'),
#     path('assigned-to-me/', AssignedToMe.as_view(), name='assigned_to_me'),
#     path('reviewing/', Reviewing.as_view(), name='reviewing'),
# ]
